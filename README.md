1. var is a function scope. and let and const is block scope when var is hoisting it return out put is undefined . and when hoisting let  type error and const reference error let and const we can not use before initialization. because before initialization it stay  temporal dead zone .

2.  the spread operator is a feature of javaScript es6. spread operator using copy array and object 

3. map:
Map takes an array, modifies each element, and returns a new array.

filter:
Filter returns a new array based on a specific condition.

forEach:
forEach does not return anything; it just loops through the array and accesses each item.

4. An arrow function is a shorter syntax for writing functions in JavaScript. It was introduced in ES6.

5 Template literals are a way to write strings in JavaScript. They allow us to use dynamic variables and expressions inside a string.